# GoChat 前端环境变量配置
# 复制此文件为 .env.local 并根据需要修改

# API 基础 URL（可选）
# 默认情况下，系统会根据访问地址自动推断后端地址
# 本地开发时访问 localhost:3000 -> 后端使用 localhost:8080
# 内网访问 192.168.1.x:3000 -> 后端使用 192.168.1.x:8080
# 如需自定义，取消注释并修改以下配置：
# REACT_APP_API_BASE_URL=http://localhost:8080/api/v1

# WebSocket URL（可选）
# 默认会根据 API URL 自动推断
# 如需自定义，取消注释并修改以下配置：
# REACT_APP_WS_URL=ws://localhost:8080/ws
